# springDataParseErrorReproducer
Reproducer to show that Spring Data 3.3.5 has a bug in its query parser.

## To Run:

```sh
./gradlew test
```

The above works using Spring Data 3.3.4 but fails with Spring Data 3.3.5.